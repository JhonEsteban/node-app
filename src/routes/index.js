const initialRoutes = require('./initial.routes');
const authRoutes = require('./auth.routes');
const userRoutes = require('./user.routes');

module.exports = {
  initialRoutes,
  authRoutes,
  userRoutes,
};
